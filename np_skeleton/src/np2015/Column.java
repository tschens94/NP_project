package np2015;

public class Column {

	/**
	 * method to initialize the graph
	 * @param row: the row in the current column
	 * @param value: the initial value
	 */
	public void addInitialNode(int row, double value) {
		
	}
}
